let base: number = parseFloat(prompt('Enter base of triangle'))
let height: number = parseFloat(prompt('Height of triangle'))

let area = 0.5 * base * height

alert(`The area of your triangle is ${area}`)